# read in objects associated with a certain project or Goal
# sort the objects in appropriate order, based on the deadlines.
# write the objects into a file, so that we can load in the TimeLine directly next time
# updating the file directly if necessary.

